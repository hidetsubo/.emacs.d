kind=added
visibility=private 
names=try_static_assert

--- try_static_assert(expr, headers = nil, opt = "") -> bool
--- try_static_assert(expr, headers = nil, opt = ""){ ... } -> bool
@todo ???

...

@param expr C 言語の式を指定します。

@param headers 追加のヘッダファイルを指定します。

@param opt コンパイラに渡すオプションを指定します。
           $CFLAGS もコンパイラには渡されます。

