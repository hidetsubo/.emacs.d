private=true
params=(VALUE val, VALUE self, VALUE klass, int pcall)
type=VALUE
name=rb_yield_0
filename=eval.c
macro=false


カレントブロックに val を渡して処理を移す。
そのさい self とクラスを self と klass に切り替える。
Proc の呼び出しのときは pcall=非ゼロ にしなければならない。

