params=(long len)
filename=array.c
name=rb_ary_new2
type=VALUE
macro=false
private=false


長さ len 分だけメモリを確保した、
長さゼロの Ruby の配列を作成し返します。

対応するRubyコード

  ary = Array.new(len)

使用例

  VALUE ary;
  long len;
  ...
  ary = rb_ary_new2(len);

